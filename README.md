# 新教师导师结对编排

教师发展中心使用本项目管理结对方案的基础资料。`fixtures/matching-context.json` 给出一组脱敏人员约束，`src/domain.js` 定义方案阶段和确认结果。学科兼容、导师容量与回避关系属于硬约束，偏好仅用于同等可行方案之间的选择。

项目要求 Node.js 20 或更高版本。执行 `npm test` 可以验证示例资料与领域常量的一致性，本地敏感配置应写入 `.env`。
